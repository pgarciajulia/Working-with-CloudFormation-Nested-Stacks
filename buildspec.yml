version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.11
    commands:
      - pip install aws-sam-cli
  build:
    commands:
      - sam build
      - sam deploy --stack-name multi-lambda-stack \
          --region us-east-1 \
          --s3-bucket arn:aws:s3:::codepipeline-us-east-1-d4e665bfbf45-4909-8df2-a6ef250f3f62 \ 
          --capabilities CAPABILITY_IAM \
          --no-confirm-changeset \
          --no-fail-on-empty-changeset